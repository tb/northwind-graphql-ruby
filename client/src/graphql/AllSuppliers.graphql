#import "./SupplierFields.graphql"

query AllSuppliers($first: Int, $offset: Int, $orderBy: String) {
  allSuppliers(
    first: $first
    offset: $offset
    orderBy: $orderBy
  ) {
    totalCount
    nodes {
      ...SupplierFields
    }
  }
}
