{
  "name": "happix",
  "private": true,
  "engines": {
    "node": "8.6.x",
    "yarn": "1.1.0"
  },
  "scripts": {
    "postinstall": "cd client && yarn && yarn build"
  },
  "cacheDirectories": [
    "client/node_modules"
  ]
}
